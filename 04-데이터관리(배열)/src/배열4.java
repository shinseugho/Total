/*
 *  관련된 내용을 묶어서 사용
 *  
 *  => 같은 데이터형 묶어서 한 개 이름으로 제어 : 배열
 *     ==========
 *     int[] arr = {'A','B','C' .....}
 *     int[] arr = new int[3]; 
 *        두 방식의 차이는 맨 처음 값이 필요하면 위의 방식을 사용하고, 나중에 값을 입력해도 되면 아래방식을 사용한다.
 *        데이터가 적어서 입력가능하면 위의 방식을 사용하고, 데이터가 많으면 아래방식을 사용한다.
 *        class의 값을 생성할 필요가 아래방식을 사용한다.  
 *     double[] arr = {10,20,30 .....}
 *     String[] arr = {"","","" .....} => 클래스 (값이 없는 경우 : null)
 *     
 *     EX)
 *     String a="aaaaa"; 
 *	   String b=null; 
 *	   String c=" "; 
 *	   a.length(); 
 *     b.length();
 *     c.length(); 
 *	   =================== 
 *	   null과 ""은 엄연히 다르다. ""은 공백이라는 문자가 배정되었지만, null은 데이터가 아예 공백상태이다.
 */

public class 배열4 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
